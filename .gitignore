# 보안 파일 (절대 올리면 안 됨)
db_config.json
secrets.json
*.key

# Google Apps Script (GAS에서 관리하므로 제외하고 싶다면)
Code.gs
appsscript.json

# 시스템 및 파이썬 캐시 파일
Thumbs.db
__pycache__/
*.pyc

# 사용자 요청 제외 항목 (로컬 보존, 리모트 삭제)
Asin-webapp/
Export_Json(ODB, LDB).lsp
projects_cache.json